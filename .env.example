# GigE Virtual Camera - Environment Configuration Template
#
# Copy this file to .env and fill in your values
# NEVER commit .env to git (it's in .gitignore)

# =============================================================================
# Apple Developer Configuration
# =============================================================================

# Your Apple Developer Team ID (10-character identifier)
# Find at: https://developer.apple.com/account/#!/membership
APPLE_TEAM_ID=YOUR_TEAM_ID_HERE

# Code signing identity (usually "Developer ID Application" for distribution)
CODE_SIGN_IDENTITY=Developer ID Application

# Bundle ID prefix (e.g., com.yourcompany)
BUNDLE_ID_PREFIX=com.yourcompany

# =============================================================================
# Notarization Configuration (for local distribution builds)
# =============================================================================

# Your Apple ID email address
NOTARIZATION_APPLE_ID=your.email@example.com

# App-specific password for notarization
# Generate at: https://appleid.apple.com/account/manage
NOTARIZATION_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Team ID for notarization (usually same as APPLE_TEAM_ID)
NOTARIZATION_TEAM_ID=YOUR_TEAM_ID_HERE

# Keychain profile name (optional, defaults to "GigE-Notarization")
NOTARIZATION_KEYCHAIN_PROFILE=GigE-Notarization

# =============================================================================
# Build Configuration (optional)
# =============================================================================

# Build configuration (Debug or Release)
BUILD_CONFIGURATION=Release

# Deployment target (minimum macOS version)
MACOSX_DEPLOYMENT_TARGET=12.3

# =============================================================================
# Developer Notes
# =============================================================================
#
# For local development builds:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your Apple Developer Team ID
# 3. Update bundle ID prefix if needed
# 4. For distribution builds, configure notarization settings
#
# For building without code signing (community contributors):
# - Set CODE_SIGN_IDENTITY="-" (dash means no signing)
# - Some features may not work without proper signing
#
# For GitHub Actions (maintainers only):
# - Don't use .env file
# - Configure secrets in GitHub Repository Settings
# - See SECRETS_SETUP.md for details
